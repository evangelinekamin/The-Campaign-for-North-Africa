"""CNA engine — event-sourced rules core for The Campaign for North Africa.

Phase 0 walking skeleton: a faithful *architecture* on a toy scenario, proving
the bones (event log, captured RNG, phased turn loop, role-scoped action,
typed rejection, invariants, deterministic replay) before any real rule values
or LLM agents are introduced. See docs/cna-llm-research-and-design-brief.md.
"""
